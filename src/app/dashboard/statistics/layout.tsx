
// This file is no longer needed and can be deleted.
// The main dashboard layout will be used.
// Keeping it to prevent build errors if referenced, will remove in cleanup.
export default function StatsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
